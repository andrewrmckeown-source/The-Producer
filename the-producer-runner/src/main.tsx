import React from 'react';
import ReactDOM from 'react-dom/client';
import TheProducer from './App';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TheProducer />
  </React.StrictMode>
);
